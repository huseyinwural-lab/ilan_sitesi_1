# FAZ-U1 Exit Criteria

## 1. Dokümantasyon
- [x] `USER_DOMAIN_SPEC.md` kilitlendi.
- [x] `ME_API_CONTRACT.md` kilitlendi.

## 2. Veritabanı
- [ ] Migration dosyası oluşturuldu (`users` tablosu genişletildi).
- [ ] Migration başarıyla uygulandı (`alembic upgrade head`).

## 3. Kod (Backend)
- [ ] `app/routers/user_panel/me.py` oluşturuldu.
- [ ] `GET /api/v1/me` endpointi çalışıyor.
- [ ] `PATCH /api/v1/me` endpointi çalışıyor.

## 4. Test
- [ ] Swagger UI üzerinden profil çekilebiliyor.
- [ ] Güncelleme işlemi veritabanına yansıyor.

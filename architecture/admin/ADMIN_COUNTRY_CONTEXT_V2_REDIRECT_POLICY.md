# ADMIN_COUNTRY_CONTEXT_V2_REDIRECT_POLICY

- Country mode is defined by URL parameter presence.
- If param removed, UI interprets it as global mode.

Future enhancement:
- If user explicitly selected Country mode, enforce redirect to last_selected_country.

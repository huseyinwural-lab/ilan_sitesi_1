# ADMIN_COUNTRY_CONTEXT_V2_LIMITATIONS

- Enforcement currently applied only to endpoints that call `resolve_admin_country_context`.
- Additional endpoints should be updated as they are implemented.
- No automatic redirect on param removal in MVP because mode is URL-derived.
